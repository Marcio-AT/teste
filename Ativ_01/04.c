#include <stdio.h>
int soma(x, y, z, w){
    int res;
    res = x, y, z, w;
    return res;
}
int main(){
    int a, b, c, d;
    scanf("%d", &a);
    scanf("%d", &b);
    scanf("%d", &c);
    scanf("%d", &d);
    printf("%d\n", soma(a, b, c, d));
    return 0;
} 
